<!DOCTYPE html>
<html>
  <head>
    <title>Journey</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css' %>
    <%= stylesheet_link_tag 'https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js' %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>

    <nav class="sidebar">
      <ul>
        <li><%= link_to "Journey", posts_path %></li>
          <li><%= link_to "Adventures", posts_path %></li>
      </ul>

     <% if !user_signed_in?%>
            <p class="sign_in"><%= link_to 'LOG IN', new_user_session_path %></p>
       <% end %>

        <% if user_signed_in? %>
           <div class="button">
             <button class="button"><%= link_to "Create Post", new_post_path%></button>
              <button class="button"><%= link_to 'Log Out', destroy_user_session_path, method: :delete %></button>
              <button class="button"><%= link_to 'Profile', edit_user_registration_path(current_user) %></button>
           </div>
           <% end %>
     </nav>
   <!-- <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to "Journey", posts_path%>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li><%= link_to "Adventures", posts_path%></li>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <li><%= link_to "Create Post", new_post_path%></li>
          </ul>
        </div>
      </div>
    </nav> -->
  <div class="container">
    <%= yield %>
  </div>

  </body>
</html>
